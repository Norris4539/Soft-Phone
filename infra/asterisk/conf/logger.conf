[general]
; Docker captures stdout, so the console channel is the log that matters.
dateformat = %F %T

[logfiles]
console => notice,warning,error
messages => notice,warning,error
; Uncomment while debugging SIP negotiation.  Very chatty — leave off normally.
; full => notice,warning,error,debug,verbose
